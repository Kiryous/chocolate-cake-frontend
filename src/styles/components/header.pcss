.header {
  overflow: hidden;
  display: flex;
  justify-content: space-between;
  /*position: fixed;*/

  &__logo {
    background: url("../img/logo.png") center no-repeat;
    background-size: 100%;
    width: 150px;
    height: 110px;
    margin: 0 0px 0 20px;
  }

  &--pink {
    width: 100%;
    background: var(--pink);
  }
}
