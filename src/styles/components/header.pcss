.header {
  display: flex;
  justify-content: space-between;

  &__logo {
    background: url("../img/logo.png") no-repeat;
  }

  &__item {
    width: 100px;
  }

  &--pink {
    width: 100%;
    background: var(--pink);
  }
}
